import { Reducer } from '../../node_modules/redux/index'
import { IPlayers, IAction, IPlayersAll } from '../types'



export const gamePlayerReducer : Reducer = (state : IPlayers , action: IAction) : IPlayers => {
  return state
}
export const allPlayerReducer : Reducer = (state : IPlayersAll , action: IAction) : IPlayersAll => {
  return state
}


